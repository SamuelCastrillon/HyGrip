package com.hyfactory.hygrip.systems;

/**
 * Stub for crane rotation logic. Can later extend EntityTickingSystem&lt;EntityStore&gt;
 * and use Query, Store, CommandBuffer when integrating with HyFactory / game ECS.
 */
public class CraneRotationSystem {

    /**
     * Placeholder for per-tick update. Replace with ECS tick once registered with the game.
     */
    public void update(float dt) {
        // TODO: integrate with HyFactory ECS (EntityTickingSystem, Query, Store, CommandBuffer)
    }
}
